#🎨 Site Logo Feature

## ✅ Что было добавлено

### **Динамический логотип сайта**

Теперь можно загружать и управлять логотипом сайта через админку!

**Возможности:**
- ✅ Загрузка логотипа для светлого фона
- ✅ Загрузка логотипа для темного фона (опционально)
- ✅ Предпросмотр обоих вариантов
- ✅ Автоматическое отображение на сайте
- ✅ Fallback на текст "ORIENT" если логотип не загружен

---

## 🔧 Изменения в коде

### **1. backend/database.py:**

Добавлена новая таблица:
```python
class ContentSiteLogo(Base):
    __tablename__ = "content_site_logo"
    
    id = Column(Integer, primary_key=True, default=1)
    logo_url = Column(String, nullable=False)
    logo_dark_url = Column(String, nullable=True)  # For dark backgrounds
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

### **2. backend/routes/content.py:**

Добавлены endpoints:
```python
# Public
GET  /api/content/logo - Получить логотип (публичный)

# Admin
GET  /api/admin/content/logo - Получить логотип (админ)
PUT  /api/admin/content/logo - Обновить логотип (админ)
```

### **3. pages/admin/Content.tsx:**

Добавлена секция "Логотип сайта":
- Загрузка логотипа для светлого фона
- Загрузка логотипа для темного фона
- Предпросмотр обоих вариантов

### **4. components/Header.tsx:**

Обновлен для использования динамического логотипа:
```tsx
const [logo, setLogo] = useState<{logoUrl: string; logoDarkUrl: string | null} | null>(null)

useEffect(() => {
  loadLogo()
}, [])

// Отображение
{logo?.logoUrl ? (
  <img src={logo.logoUrl} alt="ORIENT" className="h-8 sm:h-10 lg:h-12" />
) : (
  <div>ORIENT</div>
)}
```

### **5. services/api.ts & publicApi.ts:**

Добавлены методы:
```tsx
// Admin
getSiteLogo()
updateSiteLogo(data)

// Public
getSiteLogo()
```

---

## 🚀 Миграция

### **Запустите скрипт миграции:**

```bash
cd backend
python migrate_logo.py
```

Скрипт создаст таблицу `content_site_logo`.

---

## 📋 Как использовать

### **1. Откройте админку:**
```
http://localhost:5173/admin/content
```

### **2. Найдите секцию "Логотип сайта"**

### **3. Загрузите логотипы:**
- **Логотип (светлый фон)** - обязательный, отображается на белом фоне
- **Логотип (темный фон)** - опциональный, для темных фонов

### **4. Нажмите "Сохранить"**

### **5. Проверьте результат:**
- Логотип должен появиться в шапке сайта
- В мобильном меню
- Вместо текста "ORIENT"

---

## 🎯 Рекомендации по логотипу

### **Размеры:**
- **Ширина:** 120-200px
- **Высота:** 40-60px
- **Формат:** PNG с прозрачным фоном
- **Разрешение:** 2x для Retina дисплеев

### **Два варианта:**
1. **Светлый фон** - темный логотип для белого фона
2. **Темный фон** - светлый логотип для черного фона (опционально)

---

## 📊 API Response

### **GET /api/content/logo**

```json
{
  "logoUrl": "http://localhost:8000/uploads/logo-light.png",
  "logoDarkUrl": "http://localhost:8000/uploads/logo-dark.png"
}
```

### **Default (если не загружен):**

```json
{
  "logoUrl": "https://via.placeholder.com/150x50?text=ORIENT",
  "logoDarkUrl": null
}
```

---

## ✅ Результат

### **До:**
- ❌ Только текст "ORIENT"
- ❌ Нельзя изменить
- ❌ Нет брендинга

### **После:**
- ✅ Динамический логотип
- ✅ Управление через админку
- ✅ Два варианта (светлый/темный)
- ✅ Предпросмотр
- ✅ Fallback на текст

---

## 🎉 Готово!

Логотип сайта теперь полностью управляется через админку!
