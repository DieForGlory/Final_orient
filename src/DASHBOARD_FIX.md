# 📊 Dashboard Fix

## ✅ Исправленная проблема

### **Dashboard не отображает статистику**

**Проблема:**
- Статистика на дашборде не загружается
- Показываются нули вместо реальных данных
- Последние заказы не отображаются

**Причина:**
`Dashboard.tsx` использовал относительные пути `/api/admin/stats` вместо централизованного API service

**Исправление в `pages/admin/Dashboard.tsx`:**

```tsx
// До:
const [statsRes, ordersRes] = await Promise.all([
  fetch('/api/admin/stats', {
    headers: {
      Authorization: `Bearer ${localStorage.getItem('adminToken')}`,
    },
  }),
  fetch('/api/admin/orders/recent', {
    headers: {
      Authorization: `Bearer ${localStorage.getItem('adminToken')}`,
    },
  }),
])

// После:
import { api } from '../../services/api'

const [statsData, ordersData] = await Promise.all([
  api.getStats(),
  api.getRecentOrders(),
])
```

✅ **Теперь дашборд работает!**

---

## 📊 Что отображается на дашборде

### **Статистика (4 карточки):**
1. **Товаров** - общее количество товаров в каталоге
2. **Заказов** - общее количество заказов
3. **Выручка** - сумма завершенных заказов
4. **Пользователей** - количество зарегистрированных пользователей

### **Быстрая статистика (2 карточки):**
1. **Ожидают обработки** - заказы со статусом "pending"
2. **Завершено** - заказы со статусом "completed"

### **Последние заказы (таблица):**
- ID заказа
- Имя клиента
- Сумма заказа
- Статус (Ожидает/В обработке/Завершен/Отменен)
- Дата создания

---

## 🚀 Как проверить

### **1. Откройте дашборд:**
```
http://localhost:5173/admin/dashboard
```

### **2. Backend логи должны показать:**
```
INFO: 127.0.0.1:xxxxx - "GET /api/admin/stats HTTP/1.1" 200 OK ✅
INFO: 127.0.0.1:xxxxx - "GET /api/admin/orders/recent HTTP/1.1" 200 OK ✅
```

### **3. Должны отобразиться:**
- ✅ Количество товаров (6)
- ✅ Количество заказов (0 или больше)
- ✅ Выручка (0₽ или больше)
- ✅ Количество пользователей (1 - админ)
- ✅ Ожидающие заказы
- ✅ Завершенные заказы
- ✅ Таблица последних заказов

---

## 📋 Backend endpoints

### **GET /api/admin/stats**
Возвращает статистику:
```json
{
  "totalProducts": 6,
  "totalOrders": 0,
  "totalRevenue": 0,
  "totalUsers": 1,
  "pendingOrders": 0,
  "completedOrders": 0
}
```

### **GET /api/admin/orders/recent?limit=10**
Возвращает последние заказы:
```json
[
  {
    "id": "ORD-001",
    "customerName": "Иван Иванов",
    "total": 45900,
    "status": "pending",
    "date": "2024-01-15"
  }
]
```

---

## ✅ Результат

### **До:**
- ❌ Статистика не загружается
- ❌ Показываются нули
- ❌ Используется прямой fetch

### **После:**
- ✅ Статистика загружается
- ✅ Показываются реальные данные
- ✅ Используется централизованный API service
- ✅ Правильная обработка ошибок

---

## 🎉 Готово!

Дашборд теперь отображает всю статистику корректно!
